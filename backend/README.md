# Python Backend (FastAPI + LangGraph)

This directory will contain the Python/FastAPI server for the Shopify Support Agent.

## Phase 2 Setup
- [ ] Initialize Python virtual environment
- [ ] Install dependencies (`fastapi`, `uvicorn`, `langgraph`, etc.)
- [ ] Implement webhook listener endpoint
